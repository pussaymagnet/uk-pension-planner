/**
 * Budget state, handlers, and cloud/local sync are implemented in `BudgetProvider.jsx`.
 * Import `BudgetProvider` from this folder, or use `BudgetFeature` as the app entry.
 */
export { BudgetProvider } from './BudgetProvider';
